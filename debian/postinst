#!/bin/sh -e

if test "$1" = "configure"; then
    ENABLED=0

    test -f /etc/default/nfblock && . /etc/default/nfblock

    if [ "$ENABLED" = "0" ]; then
	exit 0
    fi

    mkdir -p /var/lib/nfblock
    wget -P/var/lib/nfblock/ -N $BLOCKLIST_URL
fi

#DEBHELPER#

exit 0
